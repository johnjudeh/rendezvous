import { LatLng } from 'react-native-maps';

const CITY_COORDINATES: { [city: string]: LatLng } = {
    London: {
        latitude: 51.5027,
        longitude: -0.1173,
    },
}

export default CITY_COORDINATES;
